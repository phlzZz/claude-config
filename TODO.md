# TODO

- [ ] **Bootstrap the MacBook onto this repo** — clone + symlink `~/.claude/{agents,skills,settings.json}` per `SETUP.md` (macOS section). The Mac currently has no clone.
- [ ] **Delete the orphaned Google Drive settings folder** — `H:\Meine Ablage\Claude\settings` (Windows) / the equivalent Drive path on the Mac. Only after every machine points `~/.claude/settings.json` at `<repo>/settings/settings.json` and nothing else references the Drive path.
